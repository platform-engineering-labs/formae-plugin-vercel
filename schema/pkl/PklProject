amends "pkl:Project"

// Package configuration for local development.
// Update baseUri and packageZipUrl when publishing to a registry.
package {
  name = "vercel"
  baseUri = "package://localhost/plugins/vercel/schema/pkl/vercel"
  version = "0.1.0"
  packageZipUrl = "https://localhost/plugins/vercel/schema/pkl/vercel@\(version).zip"
}

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.89.0"
  }
}
